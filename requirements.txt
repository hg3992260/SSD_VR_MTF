# Core
PySide6>=6.5.0
vtk>=9.2.0
numpy>=1.24.0
scipy>=1.10.0

# Medical imaging
SimpleITK>=2.2.0
nibabel>=5.0.0

# Segmentation engine
TotalSegmentator>=2.16.0
nnunetv2>=2.8.0
# dipy 需 Python>=3.11 且有 trx-python 依赖，CI(py3.10) 与基础版不装；
# SynthSeg 可选项 → 在 py3.11+ 环境单独: pip install dipy

# Image processing
scikit-image>=0.20.0
matplotlib>=3.7.0
Pillow>=10.0.0          # PyInstaller --icon 需转 .ico，截图亦用

# GUI theme
PyCt6>=6.1.0

# GPU (optional)
cupy-cuda12x>=12.0.0
